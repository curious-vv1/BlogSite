<%- include("partials/header") -%>

<h1>Home</h1>
<p>
    <%= startingContent %>
</p>
<a href="compose"><h3>Compose a post</h3></a>

 <% posts.forEach(function(post){ %> <!--forEach is an alternative method to for loop and it is handy if there are objects in array -->
     <h1><%= post.title %></h1>  
     <p><%= post.content.substring(0,100)+"..." %><a href="/posts/<%= post.title %>">Read More</a></p>
    <% }); %>
      
 



<%- include("partials/footer") -%>